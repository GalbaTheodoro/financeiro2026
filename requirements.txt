fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.5
python-multipart>=0.0.9
qrcode>=7.4
# driver do PostgreSQL (banco na nuvem). No PC, sem FIN_DATABASE_URL,
# o sistema continua usando o arquivo dados/financeiro.db e nem carrega isto.
psycopg[binary]>=3.1

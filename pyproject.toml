[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.110", "uvicorn[standard]>=0.27", "sqlalchemy>=2.0", "pydantic>=2.5", "python-multipart>=0.0.9", "qrcode>=7.4", "cryptography>=42.0", "psycopg[binary]>=3.1" ]
requires-python = "~=3.12.0"
